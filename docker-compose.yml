version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/backend/data
      - ${RESUME_FILE_PATH:-./resume}:/app/resume:ro
    env_file:
      - .env
    restart: unless-stopped
